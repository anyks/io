# Copy to .env and adjust for your environment

# Example: SOLARIS_SSH_PORT=222
SOLARIS_SSH_PORT=222
# Example: SOLARIS_SSH_OPTS="-o StrictHostKeyChecking=no"
SOLARIS_SSH_OPTS=

# SSH target in the form user@host (or Host alias from ~/.ssh/config)
SOLARIS_SSH=forman@109.172.78.94

# Remote working directory (will be created if missing)
SOLARIS_DIR=/export/home/forman/Work/io

# Build directory (relative to SOLARIS_DIR)
SOLARIS_BUILD=build/sol

# Jobs for building
SOLARIS_JOBS=8

# CMake/CTest binaries on remote (use absolute paths if needed)
SOLARIS_CMAKE=cmake
SOLARIS_CTEST=ctest

# Generator and options
SOLARIS_GENERATOR="Unix Makefiles"
SOLARIS_BUILD_TYPE=Debug

# Backend selection
SOLARIS_EVENTPORTS=ON # ON to use Event Ports (default), OFF to fallback to /dev/poll
