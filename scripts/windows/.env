# MSYS2 Windows SSH/build configuration
# This file is sourced by scripts/windows/msys2/*.sh
# Values here override script defaults.

# SSH target (informational; useful if you write remote runners)
WIN_HOST=192.168.5.128
WIN_PORT=22
WIN_USER=anyks

# Path to repository on Windows host (POSIX path under MSYS2 bash)
# Example: /c/Users/forman/io
WIN_REPO="$(pwd)"

# Build root (POSIX path under MSYS2 bash). /e/io corresponds to E:\\io
BUILD_WIN=/e/io

# Default build configuration
CONFIG=Debug

# Watcher poll interval (seconds)
INTERVAL=15

# Git operation timeouts (seconds)
FETCH_TIMEOUT=30
RESET_TIMEOUT=30

# Log every N loop iterations (1 = log every iteration)
LOOP_LOG_EVERY=1
